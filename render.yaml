services:
  - type: web
    name: ai-project-manager
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn ai_project_management.wsgi:application"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: ai_project_management.settings
